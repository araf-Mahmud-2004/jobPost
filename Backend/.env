# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb+srv://jobp:jobp1234@clusterbatt.p9bwbmo.mongodb.net/job-portal?retryWrites=true&w=majority
# JWT Configuration
JWT_SECRET=4a8dd969b1dfb19f89bd0ac628e3e7416ae829a46fb75dc166e3bc49a8bad1c0
JWT_EXPIRES_IN=30d
JWT_COOKIE_EXPIRES_IN=30

# Rate Limiting
RATE_LIMIT_WINDOW_MS=15*60*1000  # 15 minutes
RATE_LIMIT_MAX=100  # limit each IP to 100 requests per window

# CORS (comma-separated list of allowed origins)
CORS_ORIGIN=http://localhost:3000,http://localhost:5000

# Development Logging
NODE_ENV=development

# Email Configuration (for production, use real SMTP service)
# For development, you can use Mailtrap (https://mailtrap.io/)
EMAIL_HOST=sandbox.smtp.mailtrap.io
EMAIL_PORT=2525
EMAIL_USERNAME=your_mailtrap_username
EMAIL_PASSWORD=your_mailtrap_password
EMAIL_FROM='Job Portal <noreply@jobportal.com>'

# Frontend URL (for password reset links, etc.)
FRONTEND_URL=http://localhost:3000
